name: Oracle Capacity Hunter

on:
  workflow_dispatch:

permissions:
  contents: read
  actions: write

jobs:
  hunt-capacity:
    # Auto runs are disabled intentionally. Manual dispatch only.
    runs-on: ubuntu-latest
    steps:
      - name: Disabled
        run: |
          echo "Oracle Capacity Hunter automatic runs are disabled."
          echo "Use host-hunter on Oracle VM or re-enable this workflow manually."
